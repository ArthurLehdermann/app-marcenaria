/**
 * Bootstrap mínimo — lógica do editor vive em src/app/.
 * Manter este arquivo enxuto (<100 linhas); novas features médias entram em src/app/ ou src/ui/.
 */
import { createApp } from "./app/createApp";

createApp();
